const path = require('path');

module.exports = {
    "config": path.resolve("src", "config", "db-config.json"),
    "migrations-path": path.resolve("db", "migrations"),
    "seders-path": path.resolve("db", "seeders")
};